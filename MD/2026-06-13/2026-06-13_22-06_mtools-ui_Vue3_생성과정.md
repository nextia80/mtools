# 2026-06-13 22:06 - mtools-ui Vue 3 생성 과정

## 질문

`mtools-ui` Vue 3 프로젝트를 생성하는 과정에서 터미널에 나오는 선택지를 어떻게 고르면 되는지 질문함.

## 실행한 명령어

```bash
npm create vue@latest mtools-ui
```

## 선택한 옵션

### Use TypeScript?

선택:

```text
Yes
```

이유:

- Spring Boot 백엔드 API와 연동할 때 요청/응답 타입을 명확히 관리하기 좋음.
- Vue 3 프로젝트에서 장기적으로 유지보수하기 좋음.

### Select features to include in your project

처음 추천했던 기능:

```text
Router (SPA development)
Pinia (state management)
Linter (error prevention)
Prettier (code formatting)
```

실제 터미널 기록상 최종 선택:

```text
none
```

참고:

- Router, Pinia, Linter, Prettier는 나중에 필요하면 추가 가능함.
- 처음 학습하거나 가볍게 시작할 때는 `none`으로 시작해도 괜찮음.

### Select experimental features to include in your project

선택:

```text
none
```

이유:

- `Replace Prettier with Oxfmt`는 실험 기능이라 초반 프로젝트에는 비추천.
- `Vue 3.6 (beta)`는 베타 버전이라 안정적인 시작에는 비추천.

### Skip all example code and start with a blank Vue project?

선택:

```text
No
```

이유:

- Vue 기본 예제 코드가 있으면 프로젝트 구조를 이해하기 쉬움.
- 나중에 필요 없으면 예제 코드는 쉽게 삭제할 수 있음.

## 생성 결과

Vue 3 프로젝트가 아래 위치에 생성됨.

```text
/Users/yujinseok/project/mtools/mtools-ui
```

터미널 안내:

```bash
cd mtools-ui
npm install
npm run dev
```

## 다음 단계

`mtools-ui` 폴더로 이동한 뒤 패키지를 설치하고 개발 서버를 실행하면 됨.

```bash
cd mtools-ui
npm install
npm run dev
```
